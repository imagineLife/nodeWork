# Modules (Phase 2 Scaffold)

This directory is the Phase 2 landing zone for modularized API features.

Migration status:

- `tokens`: scaffolded in `src/modules/tokens` and wired in router
- `users`: planned next

Each module will be split into:

- route
- controller
- service
- repository
